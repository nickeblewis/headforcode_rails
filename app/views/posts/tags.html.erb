<% content_for :title, "##{@tag.name}" %>
<h1 class="text-4xl font-black tracking-tight text-stone-950 dark:text-white">#<%= @tag.name %></h1>
<section class="mt-8 grid gap-8 md:grid-cols-2 xl:grid-cols-3"><%= render @posts %></section>
